package ViewJavaFx;

/**
 * An interface which represents the objects which will observe the observable.
 *
 * @author Nader - 54685
 */
public interface Observer {

    /**
     * Allows to update the observers's current informations.
     */
    public void update();

}
